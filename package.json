{
  "name": "roller-coaster-presentation",
  "private": true,
  "scripts": {
    "start": "npx serve -s . -l tcp://0.0.0.0:${PORT:-3000}",
    "dev": "npx serve -s . -l tcp://localhost:3000"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
